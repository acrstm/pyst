
  <div class="title mt-5 mb-3">
    <h1>Overview</h1>
  </div>

  <% if current_user.group_id %>

  <div class="center-frames">
    <div class="frames mt-5 mb-2">
    <%= link_to "Task Manager", group_tasks_path(current_user.group_id) %>
    </div>
    <div class="frames mb-2">
    <%= link_to "Shopping List",  group_shopping_lists_path(:group_id)%>
    </div>
    <div class="frames">
      <%= link_to "Expense Manager", group_fixed_costs_path(:group_id) %>
    </div>
  </div>

  <%= link_to "Edit profile", edit_user_registration_path %>

  <% else %>

  <%= link_to "Create a group", new_group_path %>
<%= link_to "Join a group", join_groups_path %>
<%= link_to "Edit profile", edit_user_registration_path %>


  <% end %>
