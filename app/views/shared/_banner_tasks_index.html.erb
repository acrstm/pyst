<div class="banner_tasks_index">
  <div class="container d-flex justifycontent-between">
    <% @upcoming_tasks.each do |task| %>
      <div class="row"><li><%= task.name %> <%= task.deadline %> <%= task.user_id %></div>
    <% end %>
  </div>

</div>
